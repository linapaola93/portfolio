# Instalación del Proyecto - Portafolio Audiovisual

## 📦 Instalar Dependencias

Para que el proyecto funcione correctamente, necesitas instalar todas las dependencias, incluyendo Swiper.js para el slider dual.

### Opción 1: Instalar todas las dependencias
```bash
cd /Users/lcardenas/Documents/proyectos/v6/shipping/portafolio-audiovisual
npm install
```

### Opción 2: Instalar solo Swiper (si ya instalaste las demás)
```bash
cd /Users/lcardenas/Documents/proyectos/v6/shipping/portafolio-audiovisual
npm install swiper
```

## 🚀 Ejecutar el Proyecto

Una vez instaladas las dependencias:

```bash
npm run dev
```

El servidor de desarrollo se iniciará y podrás ver el portafolio en tu navegador.

## 📋 Componentes Incluidos

1. **Carousel3D** - Carrusel 3D con navegación vertical
2. **CircularGallery** - Galería circular tipo lente de cámara
3. **DualSlider** - Slider dual con thumbnails de navegación (requiere Swiper)

## ⚠️ Nota

Si encuentras problemas de permisos al ejecutar `npm install`, intenta:

```bash
sudo npm install
```

O verifica que tienes permisos de escritura en el directorio del proyecto.
